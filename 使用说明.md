# 智客服 · AI 电商客服自动化 Agent —— 使用说明

一款面向电商商家的 **桌面自动化运营中台**（2026 iCAN 大学生创新创业大赛 参赛作品）。
它由 **客服自动化** 与 **运营自动化** 双引擎组成：
通过 **Chrome 调试协议（CDP）** 接管已登录的拼多多商家后台，自动巡检未读会话，
用 **规则护栏 + 知识库 + DeepSeek 大模型** 混合决策，自动回复、静默已读或转人工；
同时内置运营引擎，实时分析经营数据（报表 / 退款归因 / 店铺体检 / ROI 监控 / 参考图库 / 命令工厂），
并把客服接待到的退款诉求自动反哺到运营分析。
在「自动化驾驶舱」与「运营中心」中实时展示每一次接管的决策链路与经营数据。

> 绿色版 zip **内置便携版 Python 3.12 与全部依赖**，解压即用、无需安装任何环境，可邮件发送。

---

## 一、解决的传统电商痛点

本产品面向**中小电商商家**，聚焦解决传统电商运营中高度重复、靠人力堆叠、数据难统一的核心痛点：

| 痛点 | 传统做法 | 本产品的解法 |
|------|----------|--------------|
| **客服响应慢、人力成本高** | 客服逐条手动回复，高峰期排队、漏回、超时被平台处罚 | 通过 CDP 自动巡检未读会话，用「规则护栏 + 知识库 + 记忆库 + DeepSeek 大模型」混合决策，**自动回复 / 静默已读 / 转人工**三级接管，7×24 值班 |
| **退款诉求处置不及时** | 退款靠人工盯，退款率与平台纠纷难控制 | **退款红线即时拦截**并醒目提示人工介入，退款诉求自动进入运营退款归因，第一时间止损 |
| **经营数据口径不一、易造假** | 手工表 / 多系统汇总，日、周、月对不上，甚至人为凑数 | 全链路统一来源 = 拼多多「交易数据-实时」**权威日统计**，无在线数据即**置空 + 标注**，绝不回退演示数据或订单拼凑冒充成交 |
| **推广投放盲目、ROI 难看清** | ROI 靠估算，成本悄悄升高不易察觉，钱花在哪不知道 | **ROI 逐计划诊断** + **实时监控告警**，自动识别「推广成本升 + 成交额降」「ROI 低于保本线」等异常，并给出分步出手建议 |
| **客服与运营割裂** | 客服看到的退款问题，运营很久之后才知道 | **客服反哺**：客服接待中的退款诉求自动计入运营退款归因，让「客服看到的退款」直接变成「运营要解决的退款」 |
| **经验沉淀困难** | 老客服离职话术流失，新人要重新学，回答不统一 | **记忆库**自动从真实回复中学习历史问答，遇到库外/相似问题直接复用经验话术，并全部过敏感词合规过滤 |
| **上线门槛高** | 中小商家没有技术团队，复杂的安装部署劝退 | **绿色版免安装**：内置便携版 Python、解压即用、可邮件分发，双击即启动驾驶舱 |

---

## 二、应用方案

根据商家所处的阶段与核心诉求，可按以下方案组合使用本产品：

**方案 1：电商客服自动化托管**
适合日咨询量较大、客服无法 24 小时值守的中小店铺。连接已登录的拼多多商家后台 → 启动引擎 → 系统周期巡检未读会话，自动处理常见咨询、静默已读小事、拦截退款红线并转人工。可先开「演练模式」安全试运行，确认决策链路无误后再真实接管。
> 对应功能：第五节「连接拼多多商家后台」、第六节「驾驶舱界面」。

**方案 2：经营数据统一口径与精确统计**
针对数据造假、口径混乱问题。通过 **CSV 导入 / CDP 同步 / 手动快照** 三种方式把真实经营数据写入本地仓库；数字用防爬图标字体时，程序内置 **Canvas 字形解码**自动还原精确数字。**所有每日 / 每周 / 每月 / 30 天成交额只采纳「交易数据-实时」权威日统计**，无在线数据即置空标注；并内置**严格对账**以权威日统计为基准校验订单明细完整性、偏差超阈值即告警，从机制上杜绝统计造假。
> 对应功能：第七节「运营中心 → 数据接入」。

**方案 3：经营分析与智能优化中台**
把店铺的成交、退款、投放、复购集中到一个驾驶舱看板：总览、经营报表、退款归因、店铺体检、ROI 监控、优化方案六大模块联动，实测对照行业基准，自动生成可执行优化动作；实时监控对每条商品链接按间隔巡检，异常即告警并附分步建议与 LLM 经营解读。
> 对应功能：第七节「运营中心」。

**方案 4：客服经验沉淀与知识复用**
把分散在客服手里的经验变成**可复用资产**。一点「一键同步客服记录」即可把工作台全部历史会话结构化入库，引擎真实运行后持续增量学习，新员工可直接检索相似问题获得一致话术，降低对个别老客服的依赖。
> 对应功能：第八节「记忆库」。

**方案 5：商品选品与主图参考**
运营需要回看同类商品主图做选品参考时，使用「参考图库」按平台 / 品类 / 关键词采集商品主图到本地图库；接入真实运营数据后也可用于商品维度分析。
> 对应功能：第七节「运营中心 → 参考图库」。

**方案 6：零门槛交付与参赛演示**
一台没有安装任何环境的新电脑也能跑起来：发一个绿色版 zip、解压、双击 `run.bat`，浏览器自动打开驾驶舱。尤其是竞赛演示场景，可用「演练模式」安全展示完整决策链路（退款拦截 / 教程自动回复 / 转人工 / 实时日志），再关闭演练模式连真实后台展示接管能力。
> 对应功能：第四节「快速启动」、第五节「连接拼多多商家后台」、文末「参赛演示建议」。

---

## 三、三种交付形态

| 形态 | 文件 | 运行环境 | 适合场景 |
|------|------|----------|----------|
| **绿色版 zip**（推荐） | `release\AiCSAgent-Green.zip` | 任意 Windows 64 位，**无需 Python** | 邮件发送、评委解压即用 |
| **一键启动器** | 绿色版内的 `一键启动.exe` | 同上 | 双击即完成「后端 + 调试浏览器 + 驾驶舱」全套启动，免逐个点击 |
| **单文件 exe** | `release\AiCSAgent.exe` | 任意 Windows，无需 Python | 本机演示、极简分发 |

> 说明：前端页面已预构建为静态文件（`frontend\dist`），运行时**不需要 Node.js**；
> Node.js 仅在开发阶段用于重新构建前端。

---

## 四、快速启动（绿色版）

**发送与解压**

1. 将 `AiCSAgent-Green.zip` 发送给对方（含视频讲解，约 160 MB；超出邮箱附件上限时改用网盘 / QQ 离线文件）
2. 对方**解压到任意目录**（建议非中文路径）。解压后请保持文件夹完整，不要再单独挪动里面的文件

**启动（四种方式任选其一，都无需安装 Python）**

| 方式 | 操作 | 说明 |
|------|------|------|
| ① 一键启动（推荐） | 双击 **`一键启动.exe`** | 单文件启动器：自动校验环境 → 启动后端 → 自动拉起调试浏览器（9222）→ 服务就绪后再打开驾驶舱，**一步到位，无需逐个点击** |
| ② 包内快捷方式 | 双击文件夹内的 **`启动智客服`** | 最常用，带程序图标，效果同 ① |
| ③ 建桌面图标 | 双击 **`创建桌面快捷方式.bat`** | 在桌面生成「启动智客服」图标，之后从桌面直接启动 |
| ④ 直接启动 | 双击 **`run.bat`** | 快捷方式被邮箱/杀软过滤掉时的兜底方式 |

启动后：控制台窗口出现启动信息 → 浏览器自动打开 http://127.0.0.1:8000 → **关闭控制台窗口即停止服务**。

> `一键启动.exe` 是独立的小启动器（约 6 MB），必须放在解压目录内（与 `run.bat` 同级）运行；它启动的服务本身仍是包内的完整程序，功能与其他方式完全一致。

> **先看视频**：`视频教程/智客服视频讲解.mp4` 为全流程操作讲解（含后台连接、数据同步、客服接管演示），上手前建议先过一遍。

> 单文件 exe 同理：双击 `AiCSAgent.exe` 即可，程序会自动拉起调试浏览器（CDP 9222）并打开驾驶舱页面，**一键启动、无需再单独运行 `start_chrome_debug.bat`**。

> **重复双击不冲突**：若程序已在运行，再次双击不会报错，会自动检测到已有实例并直接打开驾驶舱浏览器页面。

---

## 五、连接拼多多商家后台（核心功能）

系统通过 CDP 接管**已在 Chrome 中登录**的拼多多商家后台。**主程序启动时会自动拉起调试浏览器**（检测到 9222 调试端口未运行，即自动查找 Chrome/Edge 并启动），无需单独操作：

1. 双击 `AiCSAgent.exe`（绿色版则双击 `run.bat`），程序自动完成：启动调试浏览器（9222）→ 启动驾驶舱（8000）→ 自动打开驾驶舱页面
2. 在自动弹出的调试浏览器窗口中登录 `mms.pinduoduo.com` 商家账号（调试窗口需保持打开，勿关闭）
3. 回到驾驶舱，点击「**启动引擎**」→ 输入巡检间隔（默认 30 秒）→ 可开启「演练模式」先试运行
   > 点击「启动引擎」时程序会**再次检测 CDP 端口**：若调试浏览器未运行，会自动拉起（双保险，无需手动开浏览器）
4. 引擎开始周期巡检，自动接管未读会话

> 手动方式（可选）：若需自行指定浏览器路径，仍可双击 `start_chrome_debug.bat`，或直接命令行启动：

```
"C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222
```

### 演练模式（dry）
- 开启后引擎**只决策、不真实发送**，日志标注「（演练）」——适合现场演示安全红线与决策链路
- 关闭后为真实接管：自动回复、静默已读、转人工，全流程可追溯

---

## 六、驾驶舱界面

| 区域 | 功能 |
|------|------|
| 顶栏 | 店铺名 + 引擎运行状态（运行中/已停止） |
| 引擎控制卡 | 启动/停止、巡检间隔、演练模式开关、4 项核心指标（巡检轮次/自动处理/转人工/退款拦截） |
| 实时接管日志 | 每 3 秒刷新，逐条展示 巡检/决策/已回复/转人工/未识别 等事件，支持筛选与点击展开详情 |
| 决策链路明细 | 点击任意日志事件，回放该次接管的链路（会话接入 → 意图识别 → 安全护栏 → 决策动作 → 执行结果） |
| 人工接管记录 | 退款诉求与无法识别的会话，红色醒目提示人工介入 |
| 运行统计 | 处置分布 + 自动处置率 |

---

## 七、运营中心（经营数据分析）

点击顶栏「**运营中心**」页签，进入 10 大功能模块（内置演示数据，解压即可体验，无需真实店铺）：

| 模块 | 功能 |
|------|------|
| **总览** | 今日成交额/订单/退款率/健康度/整体 ROI/客服反哺 指标卡 + 环比变化 |
| **经营报表** | 日报/周报/月报/年报：成交额、订单数、客单价、退款率、新客占比、退款金额 + 趋势 |
| **退款归因** | 退款率/单数/金额/平均时长 + 退款原因分布 + 问题归类（产品/服务/物流）+ 高退款风险商品 |
| **店铺体检** | 五维打分（退款健康度/客单价/新客/投放/复购），总分与等级，实测 vs 行业基准 |
| **ROI 监控** | 各投放计划花费/产出/ROI/净利/点击率/转化率逐条诊断 + 按 ROI 加权重新分配预算建议 |
| **参考图库** | 按平台/品类/关键词采集商品主图：复用调试浏览器(9222)中已登录会话，打开对应平台搜索页截图真实主图 PNG 入库；调试浏览器未就绪时自动拉起，仍失败才生成演示占位图 |
| **命令工厂** | 店家**不写代码**，用自然语言提需求 → Agent 自动生成命令 → 安全审查 → 沙箱试运行 → 上线；支持命令进化/回滚/删除 |
| **优化方案** | 综合体检/退款/报表/ROI 自动生成可执行的店铺优化方案 |
| **数据接入** | 把商家的**真实订单与商品链接日指标**接入系统：支持商家后台导出 CSV 导入、CDP 一键同步已登录后台的表格、手动每日快照；接入后运营中心自动切换为真实数据口径 |
| **实时监控** | 按间隔自动巡检各商品链接指标，检测「推广成本升 + 成交额降」「ROI 低于保本线」等异常，每条告警附**规则建议 + LLM 经营解读**（配置 Key 后自动生成） |

> **客服反哺**：客服自动化中转人工的退款诉求事件，会自动计入运营端退款归因分析，
> 让「客服看到的退款」直接变成「运营要解决的退款」。

### 7.1 接入真实运营数据（数据接入）

「运营中心 → 数据接入」支持三种方式把商家真实经营数据写入本地仓库（`backend\data\ops_real.json`）：

| 方式 | 操作 | 说明 |
|------|------|------|
| **CSV 报表导入** | 点「报表导入」选择文件 | 直接导入商家后台导出的「订单报表 / 推广报表」CSV，自动识别表头（订单号/成交时间/实付金额/商品ID/推广花费/成交金额等） |
| **CDP 同步** | 点「CDP 同步」 | 复用调试浏览器中**已登录**的商家后台会话，自动抓取当前数据页表格并落库（需要商家后台已打开经营数据/商品数据页）。**自动同步两页数据**：①「商品数据」页的每链接日指标（金额/订单/访客/浏览）；②「交易数据」页的省份/月份交易概况 |
| **手动快照** | 点「手动快照」填行 | 每日手动录入各链接的 花费/成交/订单/点击 等指标 |

接入后「数据源状态」显示 **● 真实数据**，运营中心全部模块（总览/报表/ROI/监控）自动切换为真实口径；
清空按钮可一键回到演示数据。

**精确同步说明（CDP 自动解码）**：
- 拼多多「商品数据」页的成交金额/访客数/浏览数等数字用**防爬图标字体(spider-font)**渲染，直接读取会得到乱码。程序内置 **Canvas 字形解码**：把每个乱码字形与数字逐像素比对，**自动还原成真实数字**，同步结果精确，无需额外视觉插件。
- 「交易数据」页的**省份成交分布**（省份/金额/订单数/买家数/客单价）与**月份完成度**（各月历年成交额/目标/完成度）会作为**交易概况**自动入库，并在「数据接入」页签的「交易概况（CDP 同步）」卡片中展示。
- 同步范围选择：打开「商品数据」页点同步 → 同步商品链接日指标；打开「交易数据」页点同步 → 同步交易概况；两者互不干扰，都会自动识别。

**多周期交易数据（自动点击 · 30 分钟定时）**：
- 程序会像自动化客服一样，利用 Python 在网页版「交易数据」页上**自动点击「实时 / 昨日 / 7日 / 30日 / 周 / 月」时间按钮**，点击后逐维度抓取「交易概况」卡数值、平台官方环比（较前1月/较前1周 ↑x%）与「统计时间」区间并**独立入库**，彻底避免「30日 / 实时聚合值被当成单日口径」导致的数据对不上问题。
- 定时任务默认**每 30 分钟自动执行一轮**（由 run_server 启动时自动拉起，常驻后台运行）；也可在「数据接入」页的对应卡片随时点「**立即采集一轮**」手动触发。
- 采集结果在「数据接入」页的「**多周期交易数据 · 自动点击采集**」卡片展示：周期（实时/昨日/7日/30日/周/月）对应 成交额/订单/买家/客单价/转化率等，与「权威日统计」严格区分，互不混淆。

**报表口径（根治"与平台对不上"）**：
- **月报 / 周报 / 日报(今天、昨天) 优先直接采用平台「月 / 周 / 实时 / 昨日」按钮下的权威聚合值**，而不是把权威日统计逐日加总——日统计只覆盖程序运行以来同步过的天数，加总必然比平台整月偏小，这正是此前月报与平台对不上的根因。
- 采用前会校验该维度采集到的「统计时间」区间与报表周期是否吻合（如月报要求区间落在请求月份内），**区间不吻合一律不采用**，回退日统计加总或标注「无在线数据」，绝不拿错维度。
- 采用平台权威聚合时，报表「数据来源」会标注形如「交易数据-月·平台权威聚合（统计 09-01~09-22）」，环比也直接显示平台官方「较前1月 ↑x%」，可与网页版逐项核对。
- 「**经营数据报表**」卡片右上角新增「**一键更新数据**」按钮：点击立即跑一轮 CDP 采集（权威日统计 + 订单明细 + 多周期 4 维度）刷新本地仓库，并**基于最新数据重算当前报表**，彻底解决「报表数据对不上」问题——只要商家后台已登录，点一下即可用最新权威数据立即对齐报表口径。
- 「实时同步（跟随平台）」的默认间隔已调整为 **30 分钟**，启动后立即采集首个 30 分钟周期样本，之后按周期轮询。

**严格对账（数据自检）**：
「数据接入」页内置**严格对账**：以「交易数据-实时」**权威日统计为基准**，与本地订单明细逐日比对成交额 / 订单数 / 买家数 / 退款单数 / 退款金额，任一强可比字段偏差**超过 5%** 即判为「⚠ 有偏差」并红色高亮。用于自检明细是否完整、同步是否中途遗漏、口径是否一致——从机制上兜底防造假、防漏采。买家数因两口径统计口径常不一致（如退款买家归属差异），**仅列示比对、不触发告警**。

### 7.2 实时监控告警（核心场景示例）

「运营中心 → 实时监控」按间隔（默认 60 秒）巡检每条商品链接的日指标，自动识别三类异常：

- **成本升 + 成交降**（如：推广成本 1000→1300、成交额 5000→4000）→ 判为 ROI 恶化，给出「先看 CPC → 下调出价 10%~20% → 检查转化 → 预算下调 20% 让给高效链接」的分步建议
- **ROI 低于保本线** → 建议暂停/降价止血、复盘投放词、检查毛利
- **ROI 低于预警线** → 建议预算下调并观察、对比竞品

每条告警显示 商品/指标/环比幅度，并附**规则建议**；配置 DeepSeek Key 后还会生成**大模型经营解读**。
「立即巡检」可随时手动触发一轮，「实时告警」列表保留最近 200 条。

---

## 八、记忆库（历史问答复用）

点击顶栏「**记忆库**」页签，程序会**从客服回复记录中学习**，让引擎用积累的经验回答不同问题，数据全部保存在**自己的数据文件夹**（`backend\data\memory\memory.json`）：

| 功能 | 说明 |
|------|------|
| **自动学习** | 引擎每次**真实回复**客户后，自动把「客户问题 → 回复话术」记入记忆库 |
| **历史学习** | 点「学习历史记录」一键扫描过往回复记录（历史会话 / 引擎事件流），抽取问答对入库 |
| **检索复用** | 遇到**库外/闲聊**问题，先检索记忆库：与历史问答高度相似（相似度 ≥ 0.62）→ 直接复用历史回复，比统一兜底话术更贴心 |
| **防误答** | 相似度不足时不复用（宁缺毋滥），且所有记忆回复仍过敏感词合规过滤 |
| **记忆管理** | 查看每条记忆（问题/回复/来源/复用次数）、删除学错的条目、一键开关记忆检索（数据保留） |
| **一键同步客服记录** | 点「一键同步客服记录」，程序通过 CDP **逐一遍历工作台全部历史会话**（滚动加载、跨分组、不再只看当前面板可见会话），读取完整聊天，把「客户问题 → 客服回复」问答对写入会话库 + 记忆库。解决「很多客服没同步」的遗漏问题 |

> **使用顺序**：先点一次「一键同步客服记录」把工作台里**全部**历史客服记录读进来（比「学习历史记录」更全——那是扫本地已存的文件）→ 之后引擎真实运行自动积累 → 想验证效果可在「检索测试」框输入客户问题，实时查看是否命中及相似度。
>
> **同步与学习的区别**：`学习历史记录` = 扫描本机已保存的 `sessions.json` / `agent_events.jsonl`；`一键同步客服记录` = 实时通过 CDP 打开拼多多客服工作台，把里面所有客户会话（含尚未滚动加载的历史）逐一读取出来再入库。**若之前有客服没被记录，用它补录最有效。**

---

## 九、配置 DeepSeek API Key（可选）

> 未配置 Key 时：规则引擎 + 知识库 + **记忆库**仍可全量工作（自动回复教程、静默词、退款拦截、问答复用等）；
> 配置 Key 后解锁**复杂对话与库外新问题**的智能回复。

1. **网页配置**：驾驶舱右上角「API 配置」→ 粘贴 DeepSeek Key → 保存（立即生效）
2. **文件配置**：编辑 `backend\config.json`：

```json
{
  "store_name": "魔克摩托改装",
  "deepseek_api_key": "sk-你的key",
  "deepseek_model": "deepseek-chat",
  "enable_llm": true
}
```

---

## 十、技术架构

```
拼多多商家后台（已登录 Chrome）
        │ CDP 接管（websocket-client）
        ▼
┌──────────────────────────────────────────────┐
│ 自动化引擎 auto_keeper.py                     │
│  周期巡检 → 意图识别 → 混合决策 → 发送/已读/转人工 │
└──────────────────────────────────────────────┘
   │ 事件流（JSONL）→ 驾驶舱实时可视化
   ▼
┌──────────────────────────────────────────────┐
│ 混合决策：规则护栏 → 知识库 → 记忆库 → LLM       │
│  退款红线/静默词/教程门/品类隔离 + 敏感词合规过滤  │
│  记忆库：真实回复后增量学习 + 历史记录扫描导入     │
└──────────────────────────────────────────────┘
   │
   ▼
前端：Vue 3 + Vite 自动化驾驶舱（FastAPI 静态托管）
后端：FastAPI + Uvicorn（RESTful API + 引擎进程管理）
```

---

## 十一、开发与重新打包（仅团队开发需要）

```bash
# 1. 前端构建（需 Node.js 18+）
cd frontend
npm install
npm run build

# 2. 准备内置便携版 Python（首次 / 更新依赖时）
prepare_python.bat

# 3. 打包 exe（主程序 AiCSAgent.exe + 一键启动器 一键启动.exe，需系统 Python + PyInstaller）
build.bat

# 4. 打包绿色版 zip（内置 Python + 前端产物 + 上述 exe）
package.bat
```

> 顺序说明：`build.bat` 产出 `release\AiCSAgent.exe` 与 `release\一键启动.exe`，
> `package.bat` 会把它们一起打进 `release\AiCSAgent-Green.zip`，因此**先 build 再 package**。

---

## 十二、目录结构

```
ai-cs-agent/
├── 一键启动.exe          # 一键启动器（校验环境→起后端→拉调试浏览器→开驾驶舱，免逐个点击）
├── 启动智客服.lnk        # 绿色版启动快捷方式（打包时生成，指向 run.bat，整包移动后仍可用）
├── 创建桌面快捷方式.bat  # 一键在桌面生成带图标的启动快捷方式
├── run.bat               # 启动脚本（优先内置 Python，回退系统 Python）
├── run_server.py         # 启动入口（源码/打包通用）
├── 使用说明.md / 应用方案和使用教程.pdf   # 应用方案与使用教程（PDF 版）
├── 视频教程/             # 操作演示视频（智客服视频讲解.mp4）
├── build.bat             # 打包单文件 exe
├── package.bat           # 打包绿色版 zip
├── prepare_python.bat    # 下载并装配内置便携版 Python
├── requirements.txt
├── tools/python/         # 内置便携版 Python 3.12（绿色版运行时）
├── backend/              # FastAPI 后端
│   ├── main.py           # API 路由 + 前端静态托管 + 引擎控制接口
│   ├── agentctl.py       # 引擎进程管理 / 事件流 / 统计
│   ├── paths.py          # 打包感知路径工具（只读资源 / 可写数据分离）
│   ├── config.json       # 运行配置（含 DeepSeek Key，不入库）
│   ├── memory.py          # 记忆库（历史问答复用：学习/检索/记录）
│   ├── engine/           # 规则护栏 / 知识库 / 记忆 / LLM / 敏感词
│   ├── ops/              # 运营自动化引擎（报表/体检/ROI/图库/命令工厂）
│   ├── ops_api.py        # 运营中心 API 路由
│   └── data/             # kb.json、agent_events.jsonl、memory/（可写）
├── agent/                # 自动化引擎（auto_keeper / pdd_send / 款式识别）
├── frontend/             # Vue 3 + Vite 前端（驾驶舱）
└── legacy/               # 旧 HTML 单页（前端缺失时兜底）
```

---

## 十三、版本更新记录

| 版本 | 更新内容 |
|------|----------|
| **2026-09-24** | **新增一键启动器 `一键启动.exe`**：绿色版内双击即可自动完成「环境校验 → 启动后端 → 拉起 CDP 调试浏览器（9222）→ 服务就绪后打开驾驶舱」，无需再逐个点击启动；已在运行时重复双击不会报错，会自动检测并直接打开驾驶舱（见第四节） |
| **2026-09-23** | **重置交易数据抓取机制 · 多周期自动点击采集**：Python 自动点击网页版交易数据页「实时 / 昨日 / 7日 / 30日」时间按钮逐维度抓取并独立入库，彻底根治「聚合值被当成单日口径」导致数据对不上的问题；定时频率 **30 分钟一次**（常驻后台自动执行，可手动「立即采集一轮」），前端「数据接入」新增多周期展示卡片（见第七节 7.1） |
| **2026-09-23** | **新增严格对账**（数据自检）：以「交易数据-实时」权威日统计为基准，与本地订单明细逐日比对成交额/订单数/退款等，偏差超 5% 即红色告警，兜底防造假、防漏采（见第七节 7.1） |
| **2026-09-22** | ① **客服记录全量同步**：一键同步客服记录改为 CDP **逐一遍历工作台全部历史会话**（滚动加载 + 跨分组），修复「很多客服没同步」的遗漏问题；② **交易数据精确同步**：新增对「商品数据」页**防爬图标字体的 Canvas 字形解码**（数字精确还原，解决读取乱码/不精确），并新增「交易数据」页**省份 / 月份交易概况**自动入库 + 前端展示 |
| **记忆库** | 历史问答自动复用、检索测试、防误答过滤、一键同步等（见第八节） |

---

> **参赛演示建议**：现场先以「演练模式」跑一轮，展示 退款拦截 / 教程自动回复 / 转人工 的
> 决策链路与实时日志；再关闭演练模式连接真实商家后台，展示真实接管能力；
> 已配置 DeepSeek Key 时，可现场提问库外问题展示大模型智能。
